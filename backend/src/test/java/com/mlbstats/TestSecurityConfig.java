package com.mlbstats;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Test configuration placeholder - security is handled via application-test.yml
 * and MockMvc with security setup in BaseIntegrationTest.
 */
@TestConfiguration
public class TestSecurityConfig {
    // Security configuration is managed by the test profile
}
